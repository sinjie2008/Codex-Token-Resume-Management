# Project Notes

- Stack: PHP 8+, MySQL, vanilla JavaScript; runs under Laragon.
- Run `php tests/run.php` for local parser and policy checks.
- Preserve the existing MySQL schema/migration behavior and local Codex integration.
- Keep application times stored as UTC and displayed in `Asia/Singapore`.
